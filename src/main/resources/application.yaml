spring:
  config:
    import: "optional:file:.env[.properties]"
  mongodb:
    uri: ${MONGODB_URI}
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

app:
  cors:
    allowed-origin:  ${CORS_ALLOWED_ORIGIN}